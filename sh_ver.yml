---
- hosts: all
  gather_facts: no
  tasks:
   - name: show ver
     cisco.ios.ios_command:
       commands:
         - show version | incl Version
       register: output

   - name: print output
     debug:
       var: output.stdout_lines
